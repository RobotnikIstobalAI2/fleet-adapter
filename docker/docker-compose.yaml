---
version: "3.7"
services:
  fleet-adapter:
    image: fleet-adapter:humble
    build:
      context: ..
      dockerfile: docker/Dockerfile
    network_mode: host
    ipc: host
    stop_signal: SIGINT